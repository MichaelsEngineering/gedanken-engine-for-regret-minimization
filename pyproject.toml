[build-system]
requires = ["setuptools>=82.0.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gedanken-engine-for-regret-minimization"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.12.5", "pyyaml>=6.0.3", "rich>=15.0.0"]

[project.scripts]
rc = "src.replay:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "mypy>=1.20.1",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.10",
    "types-PyYAML>=6.0.12.20260408",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
